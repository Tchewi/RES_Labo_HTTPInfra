$(function() {
	console.log("Loading students");

        function loadStudents() {
		$.getJSON( "/api/students/", function( year ) {
			console.log(year[0]["coolEvent"]);
			var message = "Nobody is here";
                        if ( year.length > 0 ) {
				message = year[0]["coolEvent"];
                        }
                        $(".skills").text(message);
		});
	};

});